using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class New411 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("asdsasdasss");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
